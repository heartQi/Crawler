# 招聘平台可见浏览器采集工具

运行：

```powershell
python main.py
```

程序会打开可见 Chrome 浏览器，逐页滚动、采集当前页并优先点击网页上的“下一页”。

Boss直聘登录流程：

1. 可先点击“登录设置”临时输入账号和密码，也可以全部留空。
2. 开始采集后，在打开的 Chrome 中选择登录方式。
3. 手动完成短信、滑块或验证码。
4. 回到程序点击“已完成登录”。

账号密码不会写入磁盘；登录会话保存在项目的 `.browser_profile` 目录中，以便下次复用。删除该目录即可清除浏览器会话。
